#include<stdio.h>
#include<string.h>

int main()
{
     // strlen()= 'This functionm is used to show the length of strings'.
    char k1[]="sai";
    char k2[]="Halwai";
    printf("The length of k1 is:%d\n",strlen(k1));
    printf("The length of k2 is:%d\n",strlen(k2));
}
